version: '1.0'

steps:
  build-docker-spring-boot:
    type: build
    description: spring maven project
    image-name: ncodefresh/test-docker-spring-boot-push
    dockerfile: Dockerfile
    tag: latest

  push-dockerhub:
    type: push
    candidate: ${{build-docker-spring-boot}}
    tag: latest
